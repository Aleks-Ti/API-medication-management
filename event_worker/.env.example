# event_worker использует корневой .env
# docker-compose.yml и docker-compose.infra.yml автоматически переопределяют:
#   DEV=prod
#   PG_HOST=db
#   RMQ_HOST=mq
#
# Для локального запуска event_worker отдельно (вне Docker),
# скопируй корневой .env и укажи:
#   DEV=local
#   PG_HOST=localhost
